/**
 * Tokens and identifieries used for authentication and management of session
 */

/** @constant {string} ACCESS_TOKEN - the Key usedfor storing the access token in thelocalStorage */
export const ACCESS_TOKEN = "access";

/** @constant {string} REFRESH_TOKEN - the key used for storing the refresh token in localStorage */
export const REFRESH_TOKEN = "refresh"

/** @constant {string} STAFF_ID - key which is used for storing the staffid in thelocalStorage */
export const STAFF_ID = "id"